# Balanced personal profile. Install as ~/.codex/personal.config.toml.
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
